 {{#if errorMessage}}
    <h1>Date Setting Failed</h1>
    <p class="error">Error: {{errorMessage}}</p>
{{else}}
    <h1>Dates Set Successfully!</h1>
    <p>The dates for the event have been set from {{startDate}} to {{endDate}}.</p>
    <a href="/rooms" hx-get="/rooms" hx-target="#content" hx-swap="innerHTML" class="continue-button">Continue</a>
{{/if}}
