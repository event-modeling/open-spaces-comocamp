
    {{#if errorMessage}}
        <h1>Space Creation Failed</h1>
        <p class="error">Error: {{errorMessage}}</p>
    {{else}}
        <h1>Space Created Successfully!</h1>
        <p>The space named "{{spaceName}}" has been successfully created.</p>
        <a href="/set_dates" class="continue-button" hx-get="/set_dates" hx-target="#content" hx-swap="innerHTML">Continue</a>
    {{/if}}