<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vote for Sessions</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <header class="header">
        <nav class="primary-nav">
            <span class="nav-item">Setup</span> |
            <span class="nav-item">Sessions</span> |
            <span class="nav-item active">Voting</span> |
            <span class="nav-item">Schedule</span>
        </nav>
    </header>

    <main class="container">
        <h1>Vote on your favourite sessions:</h1>
        
        <section class="sessions-section">
            <form method="POST" action="/voting{{#registration_id}}?registration_id={{registration_id}}{{/registration_id}}" enctype="multipart/form-data">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Votes</th>
                            <th>Name:</th>
                            <th>Facilitation:</th>
                            <th>Topic:</th>
                            <th>Interested?</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#sessions}}
                        <tr>
                            <td>{{vote_count}}</td>
                            <td>{{name}}</td>
                            <td>{{facilitation}}</td>
                            <td>{{topic}}</td>
                            <td>
                                <input type="checkbox" name="session_{{topic}}" value="true" {{#voted}}checked{{/voted}}>
                            </td>
                        </tr>
                        {{/sessions}}
                        {{^sessions}}
                        <tr>
                            <td colspan="5" class="empty-message">No sessions available for voting</td>
                        </tr>
                        {{/sessions}}
                    </tbody>
                </table>

                <p>- refresh the page to see what the current count is!</p>

                <div class="button-group">
                    <button type="submit" class="btn btn-primary">Update your choices</button>
                </div>
            </form>
        </section>
    </main>
</body>
</html> 